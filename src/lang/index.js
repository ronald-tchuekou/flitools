import header from './header';
import product from './product';
import category from './category';

const Lang = {
    header: header,
    product: product,
    category: category,
};

export default Lang;
